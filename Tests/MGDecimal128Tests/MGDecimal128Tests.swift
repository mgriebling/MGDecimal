import XCTest
@testable import MGDecimal128

final class MGDecimal128Tests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
 //       XCTAssertEqual(MGDecimal128().text, "Hello, World!")
        MGDecimal128.test()
    }
}
